import { ContractConfig, PrismaNextConfig } from "@prisma/orm-framework/config/config-types";
import { ControlExtensionDescriptor } from "@prisma/orm-framework/components/control";
//#region ../../../3-extensions/postgres/dist/config.d.mts
//#region src/config/define-config.d.ts
interface PostgresConfigOptions {
  /** A contract file path (`.prisma` or `.ts`), or a ready `ContractConfig` such as `prisma7Schema(...)`. */
  readonly contract: string | ContractConfig;
  readonly output?: string;
  readonly db?: {
    readonly connection?: string;
  };
  readonly extensions?: readonly ControlExtensionDescriptor<'sql', 'postgres'>[];
  readonly migrations?: {
    readonly dir?: string;
  };
}
declare function defineConfig(options: PostgresConfigOptions): PrismaNextConfig<'sql', 'postgres'>;
//#endregion
//#region src/config/prisma7-schema.d.ts
/**
 * Reads a Prisma 7 `schema.prisma` (or a directory of `.prisma` files) as the
 * contract source, so Prisma 8 can adopt a database Prisma 7 still migrates.
 */
declare function prisma7Schema(schemaPath: string): ContractConfig;
//#endregion
export { type PostgresConfigOptions, defineConfig, prisma7Schema };
//# sourceMappingURL=config.d.mts.map